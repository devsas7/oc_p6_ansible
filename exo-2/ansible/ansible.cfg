[defaults]
# Use your local hosts file by default
inventory = hosts

# Disable SSH host key checking (safe for lab/Docker)
host_key_checking = False

# Use Python 3 interpreter inside the container
interpreter_python = /usr/bin/python3

# Increase SSH timeout if needed
timeout = 30

# Optional: enable colored output
stdout_callback = default
